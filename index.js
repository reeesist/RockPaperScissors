function getComputerChoice() {
let choice = ['Rock', 'Paper', 'Scissors'];
let random = choice[(Math.random() * choice.length) | 0];
return random
log.console(choice.length)
}

function playerSelection() {

}